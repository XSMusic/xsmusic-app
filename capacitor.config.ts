import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xsmusic.xs',
  appName: 'XSMusic',
  webDir: 'dist/xsmusic',
  bundledWebRuntime: false,
};

export default config;
