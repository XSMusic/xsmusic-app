import { Component } from '@angular/core';
import { MediaOneBase } from '@components';

@Component({
  selector: 'page-set',
  template: `<media-one-base type="set"></media-one-base>`,
})
export class SetPage extends MediaOneBase {}
